<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
</head>

<body>
    <h1>
        <%= title %>
    </h1>
    <section>
        
        <% if(locals.chinelos && locals.chinelos.length) { %>
            <h2>Todos os Chinnelos</h2>

            <% for(let chinelo of chinelos) { %>

                <article id="chinelo<%= chinelo.id %>" data-exclusiva="<%= chinelo.exclusiva %>">
                    <h3 style="border-bottom: 2px solid <%= chinelo.cor %>; color: <%= chinelo.cor %>;" ><%= chinelo.marca %></h3>
                    <div>
                        <p>Ano: <%= chinelo.ano %></p>
                    </div>

                </article>

                <% } %>
                    <% } else { %>
                        <h2> Sem Chinelos por hoje, volte mais tarde!</h2>

                    <% } %>
    </section>
</body>

</html>